require File.expand_path(__FILE__, '../sudoku')

describe "SudokuSolver" do
  describe "#solve" do
    it "solves the grid" do
      # solver = SudokuSolver.new(File.join(__FILE__), '..', 'grids', 'guardian', '2423.sdk')
      # expect { solver.solve }.to change(solver, :nb_cell_solved) by(81)
    end
  end
end
